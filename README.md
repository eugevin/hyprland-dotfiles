# Hyprland Setup

One script does it all: system upgrade, Hyprland stack, dotfiles, configs.

```bash
curl -fsSL -O https://raw.githubusercontent.com/eugevin/hyprland-dotfiles/main/start.sh
bash ./start.sh
```

Requires stock Arch (or Arch-based) with `pacman` package manager.
